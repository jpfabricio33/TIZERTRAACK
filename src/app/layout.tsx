import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "TirzeTrack - Monitoramento Científico da Tirzepatida",
  description: "Sua fonte confiável de informações sobre o uso seguro e científico da tirzepatida para controle de diabetes tipo 2 e obesidade.",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="pt-BR">
      <body className={inter.className}>
        {children}
      </body>
    </html>
  );
}